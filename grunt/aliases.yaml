default:
- 'server'

test:
- 'jshint'

build:
- 'clean:dist'
- 'jshint'
- 'compass:dist'
- 'uglify'
- 'copy'

deploy:
- 'build'
- 'gh-pages'

server:
- 'compass:dev'
- 'connect:dev'
- 'open:dev'
- 'watch'

server-dist:
- 'build'
- 'connect:dist'
- 'open:dist'
- 'watch'
