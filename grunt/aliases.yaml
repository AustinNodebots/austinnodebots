default:
- 'server'

test:
- 'jshint'

build:
- 'clean:dist'
- 'jshint'
- 'sass:dist'
- 'uglify'
- 'copy'

deploy:
- 'build'
- 'gh-pages'

server:
- 'sass:dev'
- 'connect:dev'
- 'open:dev'
- 'watch'

server-dist:
- 'build'
- 'connect:dist'
- 'open:dist'
- 'watch'
